environment = chrome

webdriver {
  driver = chrome
}

environments {
  chrome {
    webdriver {
      driver = chrome
      autodownload = true
      capabilities {
        browserName = "chrome"
        acceptInsecureCerts = true
        "goog:chromeOptions" {
          args = [
            "start-maximized",
            "remote-allow-origins=*",
            "test-type",
            "ignore-certificate-errors",
            "incognito",
            "disable-infobars",
            "disable-gpu",
            "disable-default-apps",
            "disable-popup-blocking"
          ]
        }
      }
    }
  }

  default {
    webdriver.base.url = "https://cesaragudelo.com.co/demosite/"
    // Asegúrate de que este URL sea correcto y se use
  }
}

serenity {
  encoding = "UTF-8"
  compress.filenames = true
  take.screenshots = FOR_EACH_ACTION
}

// Credenciales (si se necesita)
credentials {
  default {
    user.name = "juand.verag@autonoma.edu.co"
    pass = "jdvg1529"
  }
}
